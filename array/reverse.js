const und = require('underscore');
const lod = require('lodash');

/**
 * reverse - изменяет порядок следования элементов на обратный
 * _.reverse([1, 2, 3, 4]) -> [ 4, 3, 2, 1 ]
 */

// lodash
console.log("lod.reverse([1, 2, 3, 4])", lod.reverse([1, 2, 3, 4]));

// es6
console.log("[1, 2, 3, 4].reverse()", [1, 2, 3, 4].reverse());