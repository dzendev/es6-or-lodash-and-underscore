const und = require('underscore');
const lod = require('lodash');

/**
 * multiply - умножение
 * _.multiply(6, 4) -> 24
 */

// lodash
console.log("lod.multiply(6, 4)", lod.multiply(6, 4));

// es6
console.log("6 * 4", 6 * 4);